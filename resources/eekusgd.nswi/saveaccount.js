this.saveaccount = function() {

this.addmore = "" + Math.floor(Math.random() * 99999) + 1

var username = addmore + {
stars: "currentStars",
moons: "currentMoons",
diamonds: "currentDiamonds"
}
}

const save = function() {
this.currentPlayer = username
if (currentPlayer < username) {
username.repeat(1)
}

saveaccount();
save();